spring:
  application:
    name: exam  # Set your application name here
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: 81942686
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true